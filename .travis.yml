# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
sudo: required
dist: trusty
cache: packages

os:
  - linux
  - osx

before_install:
  - sudo apt-get libproj-dev libgeos-dev libgdal1-dev
