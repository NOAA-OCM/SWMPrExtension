# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
sudo: required
cache: packages

os:
  - linux
  - osx
  
addons:
  apt:
    packages:
      - libgdal-dev
      - libproj-dev
